# Exemplo de configuração de ambiente (frontend/backend)
# Copie este arquivo para `.env` e ajuste conforme necessário.

# FRONTEND
# Base da API em desenvolvimento (usada pelo React)
REACT_APP_API_BASE=http://localhost:3020/api

# BACKEND
# URL pública do app (produção). Ex.: https://seu-dominio.com/
# O servidor usará este valor para montar links públicos (tracking, QR etc.)
PUBLIC_APP_URL=

# Configuração do MySQL (HostGator)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=

# Porta do backend local
PORT=3020